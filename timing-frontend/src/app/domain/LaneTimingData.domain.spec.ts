import { LaneTimingData } from './LaneTimingData.domain';

describe('LaneTimingData', () => {
    it('should create an instance', () => {
        expect(new LaneTimingData()).toBeTruthy();
    });
});
